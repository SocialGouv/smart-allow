name: 🏷️ Version

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  id-token: write

jobs:
  version:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      # Get an ephemeral GitHub App token via SocialGouv's token-bureau.
      # This is what iterion uses. Pushes/tags made with this token trigger
      # downstream workflows (unlike the default GITHUB_TOKEN, which would
      # silently skip release.yml).
      - name: Get GitHub App token
        id: token
        uses: SocialGouv/token-bureau@main
        with:
          token-bureau-url: https://token-bureau.fabrique.social.gouv.fr
          audience: socialgouv

      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ steps.token.outputs.token }}

      - uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Install deps (pnpm via corepack)
        run: |
          corepack enable
          pnpm install --frozen-lockfile

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run release-it
        env:
          GITHUB_TOKEN: ${{ steps.token.outputs.token }}
        run: pnpm release-it --ci
